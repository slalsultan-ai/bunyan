-- Initial schema: core tables created by drizzle
-- These tables are assumed to already exist from the initial drizzle migration.
-- This migration is a no-op marker so the migration system knows where to start.

-- questions, sessions, session_answers, site_content, guest_progress
-- are created by drizzle's initial migration (0000_adorable_matthew_murdock.sql)
